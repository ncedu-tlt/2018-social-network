{
  "name": "social-network",
  "description": "Social network for developers",
  "env": {
    "REVIEW_DB_PASSWORD": {
      "required": true
    },
    "REVIEW_CLIENT_ID": {
      "required": true
    },
    "REVIEW_CLIENT_SECRET": {
      "required": true
    },
    "REVIEW_OAUTH_SERVER": {
      "required": true
    },
    "PROFILE": "review",
    "DATABASE_URL": "jdbc:h2:mem:testdb"
  },
  "buildpacks": [
    {
      "url": "heroku/java"
    }
  ]
}